#!/usr/bin/perl
sub mailwatch_get_db_name { return 'mailscanner'};
sub mailwatch_get_db_host { return 'localhost' };
sub mailwatch_get_db_user { return 'mailwatch' };
sub mailwatch_get_db_password { return 'mailwatch' };
1;
